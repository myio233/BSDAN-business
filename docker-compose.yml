services:
  bsdan-business:
    build: .
    ports:
      - "8010:8010"
    env_file:
      - .env.local
    environment:
      EXSCHOOL_HOST: 0.0.0.0
      EXSCHOOL_PORT: 8010
    volumes:
      - bsdan-storage:/app/storage

volumes:
  bsdan-storage:
